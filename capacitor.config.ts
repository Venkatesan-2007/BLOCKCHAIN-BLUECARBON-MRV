import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bluecarbonprojects.app',
  appName: 'blue-carbon-projects-dashboard',
  webDir: 'dist'
};

export default config;
